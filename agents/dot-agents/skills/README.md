Shared cross-agent skills live here.

This package is stowed under `~/.agents/` so multiple agent tools can read the
same skill definitions from one location.

Guidelines:
- put only tool-agnostic, reusable skills here
- keep tool-specific skills in the relevant tool package
- prefer one skill per directory with a `SKILL.md` entrypoint
